name = "launchia-api"
main = "src/index.ts"
compatibility_date = "2026-05-28"
compatibility_flags = ["nodejs_compat"]

# dev ポートを固定 (app の rewrites 先 INTERNAL_API_URL と一致させる)。
# 既定 8787 のままだが、明示することで「8787 が塞がっていたら別ポートに
# ずれて app と不一致になる」事故を防ぐ (塞がっていれば起動失敗してくれる)。
[dev]
port = 8787

[vars]
# 本番デプロイ時の基点（メールリンクの生成に使用）= 本番 app の URL。
# ローカル開発では .dev.vars の APP_BASE_URL=http://localhost:3000 が上書きする。
APP_BASE_URL = "https://launchia.net"
ENVIRONMENT = "production"

# Secrets (set via `wrangler secret put <NAME>`):
#   DATABASE_URL
#   RESEND_API_KEY
#   SESSION_SECRET

[env.production]
name = "launchia-api"

[env.production.vars]
APP_BASE_URL = "https://launchia.net"
ENVIRONMENT = "production"
